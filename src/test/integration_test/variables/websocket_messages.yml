query:
  all-processes: '{"process": ""}'
  process-by-id: '{"process": "${process_id}"}'
  all-instances: '{"instance": ""}'
  instance-by-id: '{"instance": "${instance_id}"}'

response:
  process-not-found: "{'Error': 'Process not found'}"
  instance-not-found: "{'Error': 'Instance not found'}"

expected_fields:
  instance:
    - process
    - Elements
    - Variables
    - Timers
    - Incidents
